<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkwechat.mapper.WeLeadsFollowerMapper">


    <!-- 获取跟进人的跟进数据  -->
    <select id="getLeadsFollower" resultType="com.linkwechat.domain.leads.leads.entity.WeLeadsFollower">
        SELECT t1.id,
               t1.leads_id,
               t1.sea_id,
               t1.follower_id,
               t1.follower_we_user_id,
               t1.follower_name,
               t1.follower_start_time,
               t1.follower_end_time
        FROM we_leads_follower t1
        WHERE t1.follower_id = #{userId}
          AND t1.follower_status = 1
    </select>
</mapper>
